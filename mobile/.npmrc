# This project sits inside the web app's repo, so npm sees the parent's
# node_modules when resolving peers and trips over react 19.2.3 (pinned by the
# Expo SDK) against the web app's react 19.2.8. The React Native ecosystem
# declares peers loosely enough that strict resolution is not meaningful here.
legacy-peer-deps=true
